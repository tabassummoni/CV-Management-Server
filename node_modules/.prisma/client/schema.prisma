datasource db {
  provider = "postgresql"
  // 👈 এখানে থাকা url লাইনটি পুরোপুরি মুছে ফেলা হয়েছে
}

generator client {
  provider = "prisma-client-js"
}

// নিচে তোমার User বা অন্যান্য Model যেভাবে আছে, সেভাবেই থাকবে
